# RAG stack — chains live in langchain-classic for LC 1.x; core 1.3+ matches classic
langchain-core>=1.3.0,<2.0
langchain-classic>=1.0.0
langchain-community>=0.3.14

# Vector store & embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Local LLM (4-bit via bitsandbytes)
transformers>=4.46.0
accelerate>=0.34.0
bitsandbytes>=0.44.0
safetensors>=0.4.0

# PyTorch: install the CUDA build matching your environment from https://pytorch.org if needed
torch>=2.2.0

# Optional app config (YAML)
pyyaml>=6.0.1

# Web UI (FastAPI + streaming)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
